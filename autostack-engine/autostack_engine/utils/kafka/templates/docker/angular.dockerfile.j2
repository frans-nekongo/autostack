FROM node:22-alpine AS build

WORKDIR /app

COPY package*.json ./
RUN npm install {{ npm_flags|default('--legacy-peer-deps') }}

COPY . .
RUN npm run build {{ build_flags|default('--prod') }}

FROM nginx:alpine

RUN rm -rf /etc/nginx/conf.d/default.conf

COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=build /app/{{ dist_dir|default('dist') }} /usr/share/nginx/html

EXPOSE {{ port|default('80') }}

CMD ["nginx", "-g", "daemon off;"]