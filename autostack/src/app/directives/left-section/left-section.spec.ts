import { LeftSection } from './left-section';

describe('LeftSection', () => {
  it('should create an instance', () => {
    const directive = new LeftSection();
    expect(directive).toBeTruthy();
  });
});
