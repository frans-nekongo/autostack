import { MainTopSection } from './main-top-section';

describe('MainTopSection', () => {
  it('should create an instance', () => {
    const directive = new MainTopSection();
    expect(directive).toBeTruthy();
  });
});
