import { BodySection } from './body-section';

describe('BodySection', () => {
  it('should create an instance', () => {
    const directive = new BodySection();
    expect(directive).toBeTruthy();
  });
});
