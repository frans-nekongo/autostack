import { TopSection } from './top-section';

describe('TopSection', () => {
  it('should create an instance', () => {
    const directive = new TopSection();
    expect(directive).toBeTruthy();
  });
});
