import { RightSection } from './right-section';

describe('RightSection', () => {
  it('should create an instance', () => {
    const directive = new RightSection();
    expect(directive).toBeTruthy();
  });
});
