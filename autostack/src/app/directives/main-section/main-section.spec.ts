import { MainSection } from './main-section';

describe('MainSection', () => {
  it('should create an instance', () => {
    const directive = new MainSection();
    expect(directive).toBeTruthy();
  });
});
